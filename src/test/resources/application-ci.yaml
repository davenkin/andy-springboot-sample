spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  cache:
    type: simple

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.12