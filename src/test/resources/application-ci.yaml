spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration # Disable Kafka
  cache:
    type: simple # Use in-memory cache instead of Redis

de: # Use embedded Mongo
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.12